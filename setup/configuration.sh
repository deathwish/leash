#!/bin/bash

# Directories
export DOWNLOAD_DIRECTORY="${LEASH_DIRECTORY}/files"
export BUILD_DIRECTORY="${LEASH_DIRECTORY}/build"
export DATA_DIRECTORY="${LEASH_DIRECTORY}/data"

# Logfiles
export BUILD_LOG="${LEASH_DIRECTORY}/build.log"